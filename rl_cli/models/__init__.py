"""Values and domain rules: what a payload, a hash, a format or a failed run is.

Read by every other package, so it reads none of them: no console, no
service, no filesystem. A rule both sides of a boundary have to agree on
belongs here rather than with either side.
"""
